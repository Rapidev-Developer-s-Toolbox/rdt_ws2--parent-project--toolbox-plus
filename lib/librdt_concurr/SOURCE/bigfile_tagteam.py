#! /usr/bin/env python

from concurrent.futures import ProcessPoolExecutor, ThreadPoolExecutor

def getexecutor(max_workers=None, thread_name_prefix='', initializer=None, initargs=()):
   return True
   
def tagteam_bychunk_read():
   '''Reads a \"chunk\" of data, its size spanning from \033[start-index: byte\033] to \033[stop-index: byte\033].'''
   with getexecutor() as exec:
      for line in args[len(*args)]:
         
def tagteam_bychunk_readlines(self, file:indexedfile, chunksize: int,  ):
   '''Reads a \"chunk\" of data, its size spanning from \033[start-index: byte\033] to \033[stop-index: byte\033].'''
   filestatservice.getalllines_firstbytes(self,file)
   with getexecutor() as exec:
      for line in args[len(*args)]:
         