#! /usr/bin/env python

from concurrent.futures import ProcessPoolExecutor, ThreadPoolExecutor
from contextlib import contextmanager

class ConcurrentExecContext:


    @contextmanager
    def getexecutor(self, executor_type='', max_w: int=None, thrname_pref: str='', initializer=None, initargs=()):
        if executor_type == 'Process': 
            self.ExecutionEnvironment.exectool = ProcessPoolExecutor(max_workers=max_w,)
        elif executor_type == 'Thread':

            return True 
    
    def tagteam_bychunk_read():
       '''Reads a \"chunk\" of data, its size spanning from \033[start-index: byte\033] to \033[stop-index: byte\033].'''
       with getexecutor() as exec:
          for line in args[len(*args)]:

    def tagteam_bychunk_readlines(self, file:indexedfile, chunksize: int,  ):
       '''Reads a \"chunk\" of data, its size spanning from \033[start-index: byte\033] to \033[stop-index: byte\033].'''
       filestatservice.getalllines_firstbytes(self,file)
       with getexecutor() as exec:
          for line in args[len(*args)]:

    #         rapidev.org, www.rapidev.org, smtp.rapidev.org,ftp.rapidev.org, git.rapidev.org, http.rapidev.org, rdt.rapidev.org, dev.  rapidev.org, download.rapidev.org, upload.rapidev.org, support.rapidev.org, administration.rapidev.org,mailing-list.rapidev.org, about.rapidev.org, community.rapidev.org
         
