#! /usr/bin/env python3

from concurrent.futures import ThreadPoolExecutor
import os

class Concurrent_Runner

def dl_with_metadata():


def _mult_proc_args_at_cmmd(args_amt, cmmd, *args):
    wrkrs = args_amt + 1
    with concurrent.futures.ThreadPoolExecutor(max_workers=wrkrs) as exec:
        future = exe.map()

def _mult_thread_args_at_cmmd(args_amt, cmmd, *args):
    pass

def _mult_proc_cmmds_at_arg(cmmds_amt, arg, *cmmds):
    wrkrs = args_amt + 1
    with concurrent.futures.ThreadPoolExecutor(max_workers=wrkrs) as exec:
        future = exe.map()

def _mult_thread_cmmds_at_arg(cmmds_amt, arg, *cmmds):
    pass
