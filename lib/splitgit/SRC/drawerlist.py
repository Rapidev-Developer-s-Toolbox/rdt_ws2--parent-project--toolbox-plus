#!/usr/bin/python

import os, pathlib
import drawer

class Drawerlist:
       
   
   def __init__(self, base):
      self.drwr_ls = []
      self.drwr_count = 0
      self.count_at_last_check = age

   def add_drwr(candidate):
      '''check that candidate is of type drawer. if no, throw TypeError'''
      True